# Opcional. La fuente de verdad es el `.env` de la raíz del monorepo
# (`envDir` de Vite apunta allí). Usa este archivo solo si corres el frontend
# aislado sin el `.env` raíz.
#
# FRONTEND_PORT=5173
# VITE_API_BASE_URL=http://localhost:8000
